[main]
# Vendor kernel/filesystem cannot store security.capability xattrs.
# Keep RPM transactions installable for this target.
tsflags=nocaps
exclude=fedora-release* generic-release* kernel-debug* setroubleshoot setroubleshoot-plugins setroubleshoot-server initial-setup* dracut-config-rescue
