[Service]
# GPU sysext units appear only after systemd-sysext merges /usr. Start either
# optional stack after that merge; the '-' prefix keeps absent stacks harmless.
ExecStartPost=-/usr/bin/systemctl start --no-block gpu-kmod.service
