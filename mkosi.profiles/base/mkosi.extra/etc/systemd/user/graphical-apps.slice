[Unit]
Description=Freezer-capable graphical application workload slice

[Slice]
# Foreground apps get priority over the shell UI, while remaining a nested
# slice that systemd can freeze and thaw as a group.
CPUWeight=200
IOWeight=200
