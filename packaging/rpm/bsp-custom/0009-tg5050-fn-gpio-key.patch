--- a/configs/pro3_linux_aiot/linux-5.15/board.dts
+++ b/configs/pro3_linux_aiot/linux-5.15/board.dts
@@ -76,6 +76,19 @@
 		regulator-name = "vibra-pwr";
 		gpio = <&pio PH 12 GPIO_ACTIVE_HIGH>;
 		enable-active-high;
+	};
+
+	/* Bottom FN switch: vendor userspace polls PL11/GPIO363. */
+	fn_switches: fn-switches {
+		compatible = "gpio-keys";
+		fn_switch: fn-switch {
+			label = "FN";
+			linux,input-type = <5>; /* EV_SW */
+			linux,code = <1>; /* SW_TABLET_MODE */
+			gpios = <&r_pio PL 11 GPIO_ACTIVE_LOW>;
+			debounce-interval = <50>;
+			wakeup-source;
+		};
 	};
 
 	standby_param: standby_param {
@@ -2409,7 +2422,8 @@
 		flash0_flvdd = "";
 		flash0_flvdd_vol = <>;
 		device_id = <0>;
-		status = "okay";
+		/* PL11 is the physical FN switch; no camera flash is fitted. */
+		status = "disabled";
 	};
 
 	sensor0:sensor@5812000 {
